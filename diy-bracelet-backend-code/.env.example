# ==================== DIY Bangle 环境变量配置 ====================
# 配置已直接写入 application-dev.yml（Docker 部署用）
# 本文件仅作为环境变量的参考说明

# ---------- 说明 ----------
# Docker 部署时，中间件之间通过 Docker 服务名通信：
#   MySQL host = mysql（docker-compose.yml 中的服务名）
#   Redis host = redis（docker-compose.yml 中的服务名）
#   MinIO 地址 = http://minio:9000
#
# 不要用 localhost！因为在 Docker 容器内，localhost 指的是容器自身

# ---------- MySQL 配置（docker-compose.yml + application-dev.yml）----------
# 密码：DiyBangle@2024

# ---------- Redis 配置 ----------
# 无密码，appendonly 持久化

# ---------- MinIO 配置（docker-compose.yml 中定义）----------
# 控制台地址：http://服务器IP:9001
# 用户名：minioadmin
# 密码：minioadmin123
